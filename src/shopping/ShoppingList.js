/**
 * @param {Object} props
 * @param {[]} props.items
 * @returns {import("react").ReactElement}
 */
export default function ShoppingList({ items }) {
  return <div>There are no items in the shopping list.</div>;
}
